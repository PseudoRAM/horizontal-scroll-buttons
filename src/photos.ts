const unsplashed = "https://source.unsplash.com/200x200/";

const photos: string[] = [
  `${unsplashed}?beach`,
  `${unsplashed}?space`,
  `${unsplashed}?office`,
  `${unsplashed}?house`,
  `${unsplashed}?shops`,
  `${unsplashed}?hat`,
  `${unsplashed}?park`,
  `${unsplashed}?food`,
  `${unsplashed}?club`,
  `${unsplashed}?dance`,
  `${unsplashed}?school`
];

export default photos;
